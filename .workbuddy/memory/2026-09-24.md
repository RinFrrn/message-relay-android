# 2026-09-24

## UI 信息架构重设计（plans/008 实施，接 2026-09-23 的方案）

- 用户点名三问题：添加/编辑交互混乱、入口层级重复、缺标准标题栏。方案见 `plans/008-ui-information-architecture.md`（三条设计规则 R1/R2/R3），当日全部落地：
  - R1：PageScaffold → 固定标题栏（返回+标题+scope徽标+actions 动作槽）+ 滚动内容区；「＋ 添加」上移标题栏（推送渠道/软件选择）。
  - R2：TemplateLibraryScreen.kt 全重写为 纯列表 + TemplateEditorDialog（弹窗新增/编辑，含预览与变量校验）；规则编辑页 TemplateSelector 就地弹窗；删 templateEditIntent 跨页编辑跳转 + SubPage.AdvancedTemplates。
  - R3：Home 删 7 张重复 FeatureCard 回归仪表盘；模板维护单入口 = 设置→消息模板一页内完成。
- compile+test+lint 全绿。CHANGELOG/plans 索引已同步。改动未提交，真机 feel check 待验。

## 去掉「全局 / 按应用」作用域标签（用户直提）

- 删除 `SettingScope.kt`（enum + ScopeBadge）；`PageScaffold` / `SectionCard` / `SettingNavRow` 的 `scope` 参数与徽标渲染全部移除，18 处 `scope = SettingScope.X` 调用点清理；设置页副标题的图例说明（「全局」对所有 App 生效…）改写；TemplateSelector 里与 SectionCard 副标题重复的一句说明删除。
- 保留的「全局」文字是解释范围的正文（如「全局默认模板」「全局行为开关」），不是标签。
- 坑：用 `perl -0pi 's/,\s*\n(\s*)scope = SettingScope\.[A-Z_]+,\s*\n/$1/g'` 批量删参数会把 `scope=` 行前的换行一起吃掉，导致 `colors,   trailing = {` 挤到同一行语法错——多行调用点必须回读修补。教训：批量正则删参数后一定要编译+回读命中的行。
- README 模板段落同步（去掉「（全局）」「（按应用）」与「入口处有标记」）。compile+test+lint 全绿。

## 软件选择 × 推送渠道关系澄清（plans/009 实施，用户拍板方案 A）

- 真实路由在 `PerAppRouteResolver.resolve`：App 绑定任意 Bark 后会【挤掉】全局目标里的 Bark（非 Bark 渠道照发）——此前 UI 完全不可见，用户以为是“额外发送”。
- A1 绑定并入渠道卡：Bark 渠道行内显示「发送范围：全部应用（默认）/ 仅 X、Y」+「绑定应用/管理绑定」按钮 → 新 `BarkBindingDialog`（弹窗，含挤掉语义提示）；页面底部独立的 BarkBindingCard 区块删除。
- A2 规则编辑页新增「发送渠道」只读卡：默认目标（主渠道名/全部渠道 N 个）+ 指定 Bark 列表 + 「指定/管理」→ 新 `AppBarkTargetsDialog`（按 App 视角勾选 Bark，与渠道页弹窗是同一份 boundAppPackages 数据的两个视角）。
- A3 多渠道开关从高级设置挪到渠道页顶部「发送到哪些渠道」卡；开启时隐藏主渠道单选钮、卡标题改「渠道列表」；高级设置只留失败重试；设置 hub 与确认弹窗文案同步。零数据迁移（DataStore 字段不动）。
- A4 设置页「软件选择」改名「转发来源（软件选择）」；渠道页副标题写明「个别 App 可在它的规则里单独指定 Bark」。
- `storedChannels`/`PackageSetSaver` private→internal（RulesScreens 跨文件使用）。
- 编译+test+lint 全绿；README 高级功能清单、CHANGELOG、plans/009 已同步。真机验收点：绑定后两页双向可见、挤掉提示可见、多渠道开关与单选互斥正确。改动未提交。

## 软件选择列表删除按钮移到详情页（用户直提）

- `SimpleAppRow` 去掉 `onDelete` 参数与行内删除按钮；`SimpleAppSelectionScreen` 删除确认弹窗一并移除。
- `AppRuleSettingsScreen` 新增 `onDeleted` 参数，页底红色描边按钮「删除这个 App 的转发规则」+ 确认弹窗；删除时顺带清理所有 Bark 渠道 `boundAppPackages` 里的该包名（防止死绑定），然后 `onDeleted()` → 调用点 `editingApp = null; pop()` 自动返回列表。
- compile+test+lint 全绿，CHANGELOG 已加条目。改动未提交。

## 首页改版：配置进度折叠 + 软件选择上首页（用户直提）

- 抽出共享 `ConfiguredAppsCard`（规则列表 + 底部「＋ 添加应用」弹窗），首页与 `SimpleAppSelectionScreen` 共用；软件选择页标题栏的「＋ 添加」action 随之移除（同一动作只留卡内一处）。
- 首次配置进度改可折叠 Surface 卡：`stepsExpanded = stepsToggled == ready`（XOR 式），ready 后自动收一行「首次配置已完成」，可再点展开复查；未 ready 时默认展开。
- 首页删「最近记录」卡（记录 Tab 已有完整列表）；Home 新增 `onOpenAppSettings` 回调直达规则编辑页。
- 坑：`pressScale(onClick, enabled = true)` 的 onClick 是第一参数，尾随 lambda 会绑到最后一参（enabled: Boolean）报错——必须写 `pressScale(onClick = { ... })`。
- compile+test+lint 全绿，CHANGELOG 已同步。改动未提交。
