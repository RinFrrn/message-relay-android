# 2026-09-23

## UX/动效/逻辑整体审计（improve-animations skill）

- 全量审计 message-relay-android，产出 `plans/` 8 个文件（000 审计 + 001-006 方案 + README 索引），未动源码。
- 18 项发现 F1-F18，根因：无 NavHost（when(subPage) 硬切）、无 rememberSaveable（pop/旋转丢状态）、无 LazyColumn（全 Column+verticalScroll）、loadInstalledApps 主线程逐应用 Binder IPC（卡顿根因）、12 内置模板 10 个字节相同、自定义模板存 Room 但零引用（templatesFlow/deleteCustomTemplate/fallbackTemplate 无调用点）、CoreLogic render() 用 require() 遇未知变量崩、消息模板 6 入口 3 数据源、三套并行规则 UI、4 个死按钮。
- 执行顺序定为 001→002→003→004→005，006 穿插。005 依赖 004（入口整合前提是自定义模板可引用）。
- 决策：不引入 NavHost/navigation-compose（已声明但零调用），用 ArrayDeque 栈 + AnimatedContent；动效参数 EaseDrawer=CubicBezier(0.32,0.72,0,1)，NAV 280ms/TAB 180ms/reduced-motion 150ms 纯淡入淡出。

## 工程经验

- Write 工具写 >300 行的大 markdown 会截断/覆盖；改用 bash `cat > file <<'PLAN_EOF'` 分段 heredoc + `wc -l` 校验，稳定。
- 用户此前会因网络中断说「请继续执行任务」；恢复后先 `wc -l`/tail 现有文件确认断点再续写。

## 方案实施（按紧急度：004 → 003 → 002）

- **004 模板系统**：`MessageTemplate.renderDetailed()` 改为非抛异常（未知变量原样保留并收集进 `unsupportedVariables`）；`TemplateCatalog` 4 个通用预设差异化（simple/standard/privacy/raw）+ `canonical()`/`displayName()`/`presetIds`；模板选择器/库/批量应用重写，自定义模板可选可删（`fallbackTemplate`+`deleteCustomTemplate`）；删掉「发送预览到渠道」死按钮；新增 `TemplateRenderTest`（7 测试）。
- **003 选择页性能**：新文件 `InstalledApps.kt`（进程级 `InstalledAppsCache` + `rememberInstalledApps()` produceState）；应用列表 LazyColumn + `derivedStateOf` 过滤；`rules.associateBy`；DAO 新查询 `recentRecords(limit)`（仅新查询，无 Migration）；PushChannelScreen 去掉 refreshKey hack。
- **002 状态恢复**：`SaveableStateProvider(key = subPage to tab)` 包住整个页面 `when`；`PageScaffold` 滚动换 `rememberSaveable(saver = ScrollState.Saver)`；4 个 Saver（AppPair/SourceSelectionList/CallTypes/PackageSet）；42 处用户输入/选择状态换 `rememberSaveable`（含 6 处带 saver）；Onboarding `step` 可保存 + `BackHandler(step>0)` 回退；`testPassed` 按计划故意不保存（防止旋转绕过测试门槛）。三个计划均 compile+testDebugUnitTest 全绿。
- 计划与 API 的两处偏差（已按真实 API 落地）：① MutableState 版重载的参数名是 `stateSaver` 不是 `saver`；② `listSaver` 返回 `Saver<Original, Any>`（不是 `Saver<Original, List<Saveable>>`），声明类型要照抄返回类型。`rememberScrollState()` 其实就是 `rememberSaveable(saver = ScrollState.Saver)`（foundation 源码确认），002 step 4 的替换是等价改写，真正让滚动跨页面存活的是 SaveableStateHolder。
- **001 导航栈与转场**：`ArrayDeque` 式 `mutableStateListOf<SubPage>` 栈 + `push()`（含 `lastOrNull() != page` 连点去重）/`pop()`；两层 `AnimatedContent`（外层 Tab 180ms 纯淡入淡出 EaseOut，内层导航 280ms EaseDrawer，push 从右 30% 滑入/旧页左退 15%，pop 严格镜像）；`rememberReducedMotion()` 读 `ANIMATOR_DURATION_SCALE`，减量模式退化为 150ms 纯淡入淡出；22 处 `subPage = SubPage.X` 改 `push(...)`；`SaveableStateProvider` key 改为内容参数 `page to activeTab`（转场期间新旧页同时成 key 必须不同，否则 `require(key !in registries)` 会崩）。计划写的 `CubicBezier(...)` 真实 API 是 `CubicBezierEasing(...)`（数值照抄）。compile+test+lint 全绿。
- 无 adb 设备，feel check（旋转/返回/列表位置 7 条）留给真机。
- 待办：005（入口整合）→ 006（死动作与打磨）。

## 方案实施（续：005 入口整合）

- **005 入口整合**：新建 `RulesScreens.kt`，迁入 `AppRuleSettingsScreen`（唯一规则编辑面）+ 重写 `Rules`（列表 + 全部启用/停用 + 行点击进编辑页，内联字段编辑全删）+ 重写 `SimpleAppRow`（整行 clickable 进编辑页 + Switch，删「设置」按钮，副标题带「近期命中 N 次」）+ 迁入并重写 `TemplateSelector`（走 `TemplateCatalog.allTemplates()` 单一来源）和 `CallTypeSelector`；`TemplateCatalog` 新增 `customTemplates()`/`allTemplates()` 统一取集合；`Database.kt` 新增 `hitCounts()` 聚合查询 + `PackageHitCount` POJO（非 Entity，无 Migration）；`templateLabel` 改为内部过滤自定义模板（新旧两种传参都兼容）。
- 005 与原计划的偏差已记录在 `plans/README.md` 表格下：导航保持 `editingApp` Pair（不动 001 接线）、TemplateLibrary 只列自定义、SimpleTemplatePresetScreen=全局默认模板行、命中数用「近期」措辞（记录会被保留策略裁剪）。
- 共享符号（UiColors/4 色/CallTypesSaver/PageScaffold/SectionCard/SettingSwitchRow/StatusBadge/PrimaryAction/EmptyText/templateLabel/defaultIncludesForTemplate）private→internal。compile+test+lint 全绿。CHANGELOG [未发布] 已补 001-005 全部用户可见变化，README 模板/规则段落已同步。

## 方案实施（续：006 死动作清理与打磨）

- **006**：F6b 重试改真发（`RelayEngine.enqueue` 走既有 RelayWorker，不新建网络栈，弹窗保留+提示）；F6c「一键诊断」无真实诊断链路 → 删；F6d LinkRow 加 url 参数 + clickable + semantics + 48dp（URL：飞书官方机器人文档 `open.feishu.cn/.../add-custom-bot`、Bark 官网 `bark.day.app`）；F11 渠道解密去重（Home 每次重组解一次 → `remember`，PushChannelScreen/reconcileUpgradeState 本就单次）；F13 无障碍基线（48dp 触摸目标、Switch 中文 semantics、状态带文字）；F14 新文件 `PrivacyDisplay.kt`（masked=详情隐藏正文，hidden=7+ 位数字串保留后 4 位）+ 高级设置「记录与隐私」两行（historyRetention 5 档 / privacyDisplayMode 3 档）；F15 BackgroundHealth 改实测三态（通知监听/通知权限/电池优化白名单/最近一次转发可判，厂商后台「检测受限」灰）；F17 删 AboutLinksSection 死代码；F18 删 navigation-compose 依赖 + darkColorScheme 直接引用 DarkUi 字段（色值零变化）。F16 早在 002 完成、F6a 早在 004 删除。compile+test+lint 全绿。
- 全部 6 个计划实施完毕；剩真机 feel check（无 adb 设备）与发版时把 CHANGELOG [未发布] 同步进 ReleaseNotes.kt。

## 模板变量参考 UI 重构（用户直提需求）

- 「高级设置 → 自定义消息模板」的变量说明由一坨多行 Text 换成新文件 `TemplateVariables.kt` 的 `TemplateVariableReference`：17 个变量按 通用(9)/电话(5)/短信(2)/其他(1) 分组、每变量单独一行（`{{name}}` 等宽字体 + 说明 + 复制按钮，行高 ≥48dp，按钮 semantics「复制变量 X」）。复制完整 `{{name}}` 到剪贴板；确认反馈 = 120ms Crossfade 图标换 Check（cubic-bezier(0.23,1,0.32,1)），1.2s 后复原，无弹跳装饰（emil-design-eng：状态指示只做微反馈）。
- `TemplateVariableDocs` 作单一来源，新增单测断言 `allNames == SUPPORTED_TEMPLATE_VARIABLES` 防文档漂移；`copyToClipboard` private→internal。设计取舍：13 正名 + 4 别名全列出（别名行注明是简写）不折叠——用户明确要「每个变量单独行」。
- compile+test+lint 全绿（仅剩 Icons.Outlined.List 既有弃用警告）。沙箱会拦 kotlin-daemon 临时标记文件的删除并让 shell 报 failed，但 Gradle 是 BUILD SUCCESSFUL，别被误导。改动未提交。

## 设置作用域标记（全局 / 按应用）

- 用户诉求：消息模板这类入口分不清是全局配置还是应用配置。方案：新增 `SettingScope.kt`（`SettingScope.GLOBAL`「全局」/ `PER_APP`「按应用」+ `ScopeBadge`，静态徽标无动效，语义走文字不靠颜色；颜色 GLOBAL=Indigo、PER_APP=Success）。
- `PageScaffold`/`SectionCard`/`SettingNavRow` 都加了可选 `scope` 参数（默认 null 不显示，全部现有调用点不受影响）。
- 标记覆盖：设置 hub 5 行（推送渠道/消息模板/免打扰/备份=全局、软件选择=按应用）+ 外观卡；高级设置 2 行（关键词过滤与应用独立规则=按应用、自定义消息模板=全局）+ 3 卡（记录与隐私/多渠道/重试=全局）；页面标题（消息模板=全局、自定义消息模板=全局、推送渠道=全局、免打扰=全局、备份=全局、软件选择=按应用、应用独立规则=按应用、App 规则编辑页=按应用）；混合作用域页里单卡标注（Bark 绑定 App=按应用）。
- 同名「消息模板」三处入口的文案全部写明生效范围：hub 行「全局默认模板，新建 App 规则时使用」；全局模板页副标题「全局默认模板…已有规则在软件选择里按 App 单独修改」；App 规则编辑页卡片「为这个 App 选择模板；模板内容是全局的」。设置 hub 副标题带图例「全局对所有 App 生效，按应用每个 App 单独一份」。
- 取舍：整页同作用域的卡不再重复标（页面标题徽标已覆盖），只有 Bark 绑定这种「全局页里的应用级卡」才单独标。README 模板节、CHANGELOG [未发布] 已同步。compile+test+lint 全绿。改动未提交。

## 空白推送排查与修复（用户报「一条微信消息多出一条空白推送」）

- 根因：`RelayNotificationService` 第 47 行的空值守卫（title 与 body 都空则丢弃）是**死代码**——`WeChatNotificationParser.parse` 无内容时填 title=「微信」/body=「该通知未提供正文」，`NotificationContentExtractor.extract` 无正文时也填「该通知未提供正文」，守卫永远不触发。微信在填充内容前/更新清空字段时发出的无会话名、无正文占位通知因此被照常转发；`DedupeWindow` 按 pkg|title|body 判重，占位内容与真实内容不同拦不住 → 一条消息两条推送，其中一条无实质内容。
- 修复（最窄、恢复守卫原意）：新增 `isBlankNotificationContent(title, body)` 纯函数（NotificationContentExtractor.kt 顶层）；`parse`/`extract` 改为可空返回并**在填占位文案之前**判空，空白通知返回 null；监听器 `?: return` 丢弃。有名字没正文、或有正文没名字的通知仍照旧转发（占位文案保留）。
- 测试：新增 `BlankNotificationGuardTest`（JVM 单测，可跑）+ `WeChatParserTest` 两条仪器用例（空通知拒收 / 仅标题保留占位正文）。compile 主代码+androidTest、testDebugUnitTest、lintDebug 全绿。
- 待真机确认：修后空白推送应消失；若仍出现，去「记录」页看多出来那条的标题/正文发回来（判断占位通知是否带 EXTRA_TITLE 等部分内容的变体，需要再收紧条件）。CHANGELOG [未发布] 已补条目。改动未提交。

## 记录列表/详情弹窗样式改造（用户：弹窗看不到过滤原因，状态要单独显示）

- 根因：`ChannelResultParser.detailText`（含 渠道/状态/原因，自带脱敏）写好了但**零调用点**，弹窗从没接入 → 看不到原因。
- 改动（MainActivity.kt）：① 记录列表行由「App · 状态」混合文本改为 Row + 独立 `StatusBadge`（`recordStatusColor`：成功=Success、已过滤/部分成功=Warning、其余=Danger；徽标文字承载语义，颜色仅辅助）；已过滤行在列表直接显示单行「原因：…」。② 详情弹窗标题改 App 名 + 状态徽标；正文在时间后接：已过滤 → `filterReason()` 高亮「过滤原因」（Warning 加粗），非已过滤 → `detailText()` 分渠道结果块。
- `ChannelResultParser.filterReason` 新增：只认 `success == null` 的形状（addFilteredRecord 的 {"reason":…}），失败记录（success=false）不误判，走 detailText；reason 过 redactResultText 脱敏。
- 新增 `ChannelResultParserTest`（3 测试）。compile+test+lint 全绿。CHANGELOG [未发布] 修复+优化各一条。改动未提交。

## 微信 {{app}}/{{title}} 同值重复修复（用户：{{app}}：{{title}} 输出「微信 · 张三：微信 · 张三」）

- 根因两处合谋：① `WeChatNotificationParser` title 拼「微信 · 」前缀；② `RelayNotificationService` 里 `relayApp`（ab45281 引入）把该 title **原样镜像进 message.app** → app == title。
- 修复：title 改为只放会话/发送人（`displayName ?: "微信"`，不带前缀）；删掉 relayApp 镜像，app 恒为应用标签「微信」。输出变为「微信：张三」（群聊「微信：群名」）。
- 连带影响（已写进 CHANGELOG）：预设模板微信标题由「微信 · 发送人」变「微信」（与 QQ 等应用一致，要发送人用自定义模板加 {{title}}）；记录列表 App 列变「微信」（会话在标题列）；历史记录保持原值。别名归一约束（wechat 别名渲染必须等同 standard）决定不能单独改 wechat 预设条目，故接受此取舍。
- 仪器测试补 title 断言（私聊=张三、群聊=测试群）。compile 主+androidTest、test、lint 全绿。改动未提交。

## 设置页直观化（plans/007 实施，用户拍板 B 全做 + 4 组）

- 问题 P1-P7：静态副标题看不出状态、免打扰副标题二选一、图标重复（Notifications×3/CheckCircle×4/List×3/Tune×2）、SIM 高低频同权、模板/规则双入口跨层级、弹窗文案漂移。
- 实施（MainActivity.kt 13 步串行编辑 T1-T13）：① `SettingNavRow` 加可选 `trailing` 插槽（默认 null 零破坏）+ 免打扰尾部 StatusBadge；② 动态副标题：软件选择「N 个 App 启用转发」（rulesFlow）、消息模板「默认：X · 自定义 N 个」（templatesFlow+DataStore）、推送渠道空时 Danger 徽标「未配置」、记录与隐私「保留 X · 隐私模式」；③ 图标一行一语义重映射（Palette/Apps/Description/Schedule/History/Save/PlayCircle/SimCard/Tune/MenuBook/Update/Code/BugReport/Info/Save）；④ 4 组分组：转发设置/推送与数据/应用/高级设置（帮助与关于不变），SIM 下沉；⑤ 模板单入口（模板页加「自定义模板库」行进 AdvancedTemplates）、规则单入口（软件选择页头加「批量管理规则」进 Rules，高级删对应行）；⑥ 新 `RecordPrivacyScreen` 从 Advanced 抽出提为一级（privacyLabel helper）；⑦ Advanced 收敛为 风险提示+多渠道+失败重试，副标题与确认弹窗文案同步。
- 路由：新增 `SubPage.RecordPrivacy`；`SimpleAppSelectionScreen` 加 `onOpenRules`、`SimpleTemplatePresetScreen` 加 `onOpenCustomTemplates` 参数（call sites 同步）。
- 编译+testDebugUnitTest+lintDebug 全绿（仅既有 Icons.Outlined.List 弃用警告）。README 三处（高级功能清单、模板入口、记录保留入口）+ CHANGELOG [未发布] 已同步；plans/007 与 plans/README 状态翻转。真机 feel check 待验。改动未提交。
- README 顺带修正：「发送延迟 / 通知合并」在本仓库从未有过 UI 入口（setNotificationTiming 只有 ConfigBackup 与 repository 调用，001-006 之前也一样），从高级功能清单移除，避免事实不一致。

## 「完整显示了但通知还是隐藏」排查（用户报）

- 结论：两个独立的「隐私」被用户混在一起。① **推送内容**由该 App 规则选的**模板**渲染：隐私模板正文恒为 `🔒 内容已隐藏`（TemplateCatalog PRIVACY_BODY），与隐私显示模式无关；新规则 templateId = recommend() 或全局默认 templatePreset（RulesScreens:265），全局默认若选了隐私模板则新建规则都隐藏。② **本机记录显示**才是 privacyDisplayMode（full/masked/hidden，PrivacyDisplay 只做展示层）。记录没正文的另一独立原因：保留策略 status_only 时**写入时 body 就存空串**（RelayEngine 三处 addRecord 都过 shouldKeepBody），改回 full 救不回历史；且列表行只显示标题，正文在详情弹窗。
- 代码里没有「已隐藏敏感信息」原句，最接近候选即「🔒 内容已隐藏」（模板）与「正文已隐藏」（masked 显示）——已按模板路径给解法。
- 顺带抓到 007 漏网过期文案：RulesScreens.kt 两处「高级设置 → 自定义消息模板」→ 已改「设置 → 消息模板 → 自定义模板库」（replace_all 一次改两处）；RecordPrivacyScreen 补一句区分说明（推送内容由模板决定）。CHANGELOG [未发布] 补一条修复。build 96SwCf 待验（后确认 BUILD SUCCESSFUL）。

## 自定义模板补编辑功能（用户报「只有添加没有编辑」）

- 确认属实：`TemplateLibrary` 保存永远生成 `custom_${timestamp}` 新 id，已保存列表只有「删除」，点模板行无任何动作。数据层本来就支持编辑（`dao.saveTemplate` 是 REPLACE upsert，同 id 覆盖后引用它的规则下次推送即生效、零 Migration）——纯 UI 缺失。
- 落地：按 AGENTS 规则 1 把 `TemplateLibrary` 从 MainActivity（已 1800+ 行）拆到新文件 **TemplateLibraryScreen.kt** 并加编辑态：`editingId`（rememberSaveable）空=新增、非空=编辑；编辑按钮载入表单 + name 字段 FocusRequester.requestFocus()（靠 TextField 自带 bring-into-view 滚到表单）；主按钮动态「保存模板/更新模板」；行内「编辑中」徽标（Indigo）；「取消编辑，恢复新增」重置表单；删除正在编辑的模板时同步退出编辑态；`LaunchedEffect(editingId, templates)` 安全网（编辑期间被别处删除→退出编辑态转新建，不复活已删 id；`templates.isNotEmpty()` 防 Room 首帧空列表误重置）。
- 配套：`previewMessage()` private→internal（新文件与 SimpleTemplatePresetScreen 共用）；新文件用 `Icons.AutoMirrored.Outlined.List`（消一个弃用警告）；CHANGELOG [未发布] 补优化条目。MainActivity:950 入口副标题「新建、编辑和删除模板」从此名副其实。
- 编译+test+lint 实为 BUILD SUCCESSFUL（沙箱拦 kotlin-daemon tmp 删除导致任务状态报 failed，看 stdout/exit=0，老假阳性）。内置模板仍不可编辑（ensureTemplates 每次启动回写内置内容，这是设计而非缺陷）。改动未提交。

## 「已过滤」原因精确化（用户问敏感词过滤怎么处理）

- 显示链路上一轮已通（列表行内原因 + 详情弹窗 filterReason），本轮补的是**原因内容**：原两处调用点（RelayEngine:24/82，process 与 processSmsEvent）共用文案「规则未命中或命中排除关键词」，看不出是哪个敏感词。
- `CoreLogic.kt`：`RelayRule` 新增 `filterReason(message): String?`（null=通过），`matches()` 改为 `filterReason == null`（布尔语义完全等价，既有测试不受影响）。原因分级：包未启用 →「未启用该应用的转发」；排除命中 →「命中排除关键词「××」」（**排除优先于包含报告**——用户主动设的敏感词更需要知道）；包含未中 →「未命中包含关键词（需要包含：…）」。
- RelayEngine 两处改用 filterReason（同文本 replace_all 一次改两处）。RulesScreens 关键词规则卡副标题同步「记录里会写明命中的具体关键词」。CoreLogicTest 新增 5 断言用例（null/排除/优先级/包含/包未启用）。CHANGELOG 补优化条目。
- 脱敏兜底：原因文本仍走 ChannelResultParser 的 redactResultText（URL→***、16+ 位字母数字→***、11 位手机号→3****4），关键词本身是手机号时显示打码版。注意：命中词来自**排除规则**、包含词列表也原样进 reason，二者都是用户自己配置的词，不是消息内容，不碰 AGENTS 6（那条管 Token/真实号码/验证码进日志）。改动未提交。
