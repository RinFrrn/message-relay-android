# 更新日志

## [未发布]

### 新增

- 页面转场动画：子页面进入/返回采用滑动加淡入淡出，Tab 切换使用淡入淡出；系统关闭动画时自动降级为无位移淡入淡出。
- 软件选择页的 App 行显示近期转发条数。
- 设置新增一级入口「记录与隐私」：历史记录保留（7 天 / 30 天 / 90 天 / 永久 / 仅状态）和隐私显示模式（完整显示 / 隐藏正文 / 隐藏号码）随时可改，不再埋在高级设置的确认弹窗之后。
- 记录详情的「重新发送 / 仍然发送 / 立即重试」改为真重新发送：走既有发送链路，弹窗保留并提示「已重新发送」。
- 配置参考链接（飞书自定义机器人文档、Bark 官网）可以点击打开。

### 优化

- 打通自定义消息模板：保存后即可在规则里选择；内置预设差异化，隐私模板真正隐藏内容，原始通知模板展示原始标题和正文。
- 消息模板入口收敛为一个：「设置 → 消息模板」页内含全局默认模板选择与「自定义模板库」（负责增删改），规则编辑页统一选择模板；所有入口看到的模板选项保持一致。
- 设置页直观化：设置项按任务频次分为 转发设置 / 推送与数据 / 应用 / 高级设置 四组（帮助与关于不变，SIM 卡管理下沉到「应用」）；每行副标题直读当前状态——启用转发的 App 数、默认模板与自定义数量、免打扰时段、记录保留时长与隐私模式、推送渠道配置情况（未配置显示红色徽标）；一行一图标不再重复；免打扰行改为「描述恒显 + 状态徽标」。入口收敛：规则相关只走「软件选择」（页内新增「批量管理规则」），模板相关只走「消息模板」（页内新增「自定义模板库」），「记录与隐私」从高级设置提升为一级入口；高级设置只保留多渠道同时发送与失败自动重试，确认弹窗文案同步对齐。
- 规则配置统一到规则编辑页：软件选择的 App 行点整行进入编辑页，行内开关只负责启用/停用；规则页提供一键全部启用/停用，不再内联编辑规则字段。
- 应用选择页滚动更流畅：读取安装应用不再逐项占用主线程，列表重组更轻。
- 旋转屏幕、切换后台返回后，当前页面、搜索词和未保存的输入会保留。
- 后台运行页只显示本机可实测的项目（通知访问权限、应用通知权限、电池优化白名单、最近一次转发）；厂商后台能力如实标注「检测受限」，不再一律显示警告色。
- 隐私显示模式生效于记录列表和详情：「隐藏正文」在详情里隐藏正文，「隐藏号码」把 7 位以上数字串保留后 4 位打码；打码后的原值不写入日志。
- 无障碍基线：可点击行触摸目标不小于 48dp，转发开关补充中文语义说明，状态不只靠颜色区分。
- 设置入口统一标注作用域「全局 / 按应用」：全局项对所有 App 生效，按应用项每个 App 单独一份；同名的「消息模板」入口（全局默认模板 vs App 规则内选择）副标题写明各自生效范围。
- 渠道配置读取不再重复解密，进入相关页面更快。
- 记录列表的发送状态从「App 名 · 状态」混合文本改为独立徽标（成功绿 / 已过滤与部分成功黄 / 失败红），已过滤的消息在列表里直接显示过滤原因。
- 明暗主题色板合并为单一来源，颜色取值不变；删除未使用的 navigation-compose 依赖和无响应的「一键诊断」按钮、死代码「关于与链接」。

### 修复

- 修复微信消息里 `{{app}}` 和 `{{title}}` 是同一段文本、`{{app}}：{{title}}` 渲染成「微信 · 张三：微信 · 张三」的重复问题：`{{app}}` 恒为应用名（微信），发送人/群名只进 `{{title}}`，现在输出「微信：张三」。影响：预设模板的微信标题随之与其它应用一致（只显示应用名，要发送人用自定义模板加 `{{title}}`），记录列表的 App 列也从「微信 · 会话」变为「微信」（会话显示在标题列）；历史记录保持原值。
- 记录详情弹窗终于能看到发送结果与过滤原因：接入早已写好但从未调用的解析函数；已过滤记录显示「过滤原因」，成功/失败记录显示各渠道结果与失败原因。
- 修复一条微信消息可能额外带出一条空白推送的问题：无会话名且无正文的空白通知（微信在填充内容前发出的占位通知）不再转发，此前它们会被占位文案顶过空值守卫。
- 修复自定义模板保存后在选择器里选不到的问题。
- 修复未知模板变量可能导致的渲染异常：不支持的变量会提示并原样保留。
- 修复模板选项在不同入口不一致的问题。
- 修复记录详情「重新发送」只关弹窗、不实际发送的问题。
- 修复配置参考链接点不动的问题。

## [0.1.3] - 2026-08-10

### 新增

- Bark 设备支持绑定 App。绑定后，该 App 的 Bark 推送只发往已绑定的有效 Bark。
- 软件选择页每个 App 增加明确的 `设置` 入口。
- App 设置页集中展示是否转发、仅息屏时推送、模板、包含关键词和排除关键词。
- SIM 卡管理页显示电话权限、联系人权限、通话记录权限、Telephony 支持状态和可读取的 SIM 信息。

### 优化

- 首页快捷卡片改为更稳的单列布局，改善大字体和窄屏下的文字裁切。
- 覆盖更新时，如果已有渠道或规则，会自动恢复为已完成配置。
- 主渠道 ID 失效时自动指向第一个有效渠道。
- 推送渠道页在加密渠道读取失败时给出明确提示。
- Bark 绑定随渠道备份和恢复，旧备份缺字段时默认未绑定。

### 修复

- 修复“仅息屏时推送”入口难找的问题。
- 修复覆盖更新后可能重新进入首次配置的问题。
- 修复 SecureStore 解密失败可能导致渠道页崩溃的问题。
- 修复 Manifest label、默认关键词、模板目录、过滤原因和发布文档中的中文乱码。
- 修复软件选择推荐区可能误显示系统组件的问题。

## [0.1.2] - 2026-08-10

### 新增

- 新增 GitHub 开源项目入口。
- 新增版本更新页面。
- 支持手动检查 GitHub Releases。
- 支持可关闭的自动检查更新，默认开启。
- 发现新版本后可前往 GitHub Release 页面自行下载。

### 优化

- 关于页面新增开源项目、开发方式和 GPL-3.0-only 许可信息。
- 更新日志新增 GitHub Releases 入口。
- 使用教程补充版本更新说明。

### 修复

- 修复应用内更新日志、测试断言和 Manifest 标签中的中文乱码。
- 修复版本比较可能误判 `0.10.0` 与 `0.9.0` 的问题。
- 更新检查失败不会影响核心消息转发流程。

## [0.1.1] - 2026-08-08

### 新增

- 保留全局多 Bark 设备配置，支持名称、声音、图标、分组、提醒等级和持续响铃参数。
- 保留每应用“仅息屏时推送”和电话通知类型设置。

### 优化

- 从软件选择、App 独立设置和高级应用规则中移除旧版“应用独立 Bark”入口，避免小白误配。
- 新备份不再导出已废弃的旧版应用独立 Bark 字段。
- 旧备份或旧数据库中的旧版独立 Bark 字段仍可读取，但导入后会禁用。

### 修复

- 修复旧规则中 `useIndependentBarkRoute=true` 可能继续影响路由的风险。
- 修复旧备份导入后可能重新启用旧版应用独立 Bark 的风险。

## [0.1.0] - 2026-08-06

### 新增

- 首页、记录、设置三页结构。
- Bark、飞书、钉钉推送渠道。
- 来源应用选择、手动包名兜底、设备应用列表权限提示。
- 简洁、标准、隐私、原始通知四套模板，以及高级自定义模板。
- 关键词过滤、排除关键词和应用独立规则。
- 每应用“仅息屏时推送”开关。
- 电话通知类型：未接来电、来电提醒、来电已接通。
- SIM 管理和 SIM 本地别名。
- 免打扰、重要消息例外、通知合并与延迟。
- 转发记录、记录详情操作和记录保留策略。
- 基础配置备份与恢复。
- 深色、浅色、跟随系统主题。
- 应用内使用教程、更新日志、关于链接和脱敏诊断。
- Launcher 图标和桌面小组件样式。

### 优化

- 首页改为更适合小白的配置状态与快捷入口。
- 设置页按基础设置、高级设置、帮助与关于分组。
- 链接行只显示名称，避免小屏幕 URL 截断。
- 时间显示统一为本地中文格式。
- 息屏判断优先使用 Display 状态，PowerManager 仅作为诊断字段。
- 电话监听按 SIM 注册，并在权限缺失时给出明确提示。
- 备份导入先校验结构和版本，失败不覆盖当前配置。
- 诊断信息避免输出完整 Token、Webhook、电话号码和验证码。

### 修复

- 修复模板变量正则导致本地预览和渠道预览报错的问题。
- 修复无已保存渠道时发送预览提示不明确的问题。
- 修复记录中“已过滤”标签缺少真实过滤记录的问题。
- 修复损坏渠道 JSON 可能影响模板预览的问题。
- 修复部分深色模式下卡片、按钮和底部导航对比不足的问题。

### 已知问题

- 电话、双卡、AOD/Doze 和不同品牌后台保活仍需要更多真机验证。
- 来电号码、联系人、归属地和运营商信息受系统权限、Android 版本和厂商限制影响。
- 离线号码归属地可能因携号转网不准确。
- 当前版本为 Beta，不建议标记为 Stable。
